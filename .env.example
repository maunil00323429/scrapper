# Copy this file to ".env" in the project root and set your values.
# Never commit .env — it is listed in .gitignore.
#
# Required for OpenAI-powered features (abstractive summary, page Q&A, /chat API).
# Leave empty to use extractive summarization only and disabled chat.

OPENAI_API_KEY=
